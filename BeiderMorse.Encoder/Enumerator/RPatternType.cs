namespace BeiderMorse.Encoder.Enumerator
{
    public enum RPatternType
    {
        EXACT,
        FIRST_CHAR,
        LAST_CHAR,
        REGEX,
        EMPTY,
        EQUALS,
        All,
        START,
        END
    }
}
